// import useFetchUserBooks from "@/app/hooks/useFetchUserBooks";
// import { useEffect, useState } from "react";
// import userGetUser from "./useGetUser";
// export default function useGetBookStatus(status: string) {
//   const [bookIds, setBookIds] = useState<string[]>([]);
//   const [datesAdded, setDatesAdded] = useState<Date[]>([]);

//   useEffect(() => {
//     const fetchBooks = () => {
//       const { userId } = userGetUser();
//       if (!userId) return;

//       const books = useFetchUserBooks(userId);
//     };
//   });
// }
